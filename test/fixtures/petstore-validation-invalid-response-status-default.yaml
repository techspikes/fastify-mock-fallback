openapi: 3.0.4
info:
  title: Invalid Response Status
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        default:
          description: response
          content:
            application/json:
              example: []
